const Wrapper = (props)=>{

/* childeren props has can hold all the content
 that has passing from opening and clonging tags
 of our coustem component

*/
return props.children
};

export default Wrapper;